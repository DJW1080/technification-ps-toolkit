<Page
    x:Class="TechnificationToolboxApp.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="{StaticResource PagePadding}"
          RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Technification Toolbox"
                   Style="{StaticResource PageTitleTextBlockStyle}" />

        <TextBlock Grid.Row="1"
                   Margin="0,4,0,0"
                   Text="All six modules now open as native WinUI pages. The legacy scripts remain available as fallback launch paths."
                   TextWrapping="Wrap"
                   Style="{StaticResource SecondaryTextBlockStyle}" />

        <CommandBar Grid.Row="2"
                    Style="{StaticResource CompactCommandBarStyle}">
            <AppBarButton Label="Toolbox"
                          Click="LaunchToolboxButton_Click">
                <AppBarButton.Icon>
                    <SymbolIcon Symbol="OpenFile" />
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton Label="Logs"
                          Click="OpenLogsButton_Click">
                <AppBarButton.Icon>
                    <SymbolIcon Symbol="Folder" />
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton Label="Reports"
                          Click="OpenReportsButton_Click">
                <AppBarButton.Icon>
                    <SymbolIcon Symbol="Document" />
                </AppBarButton.Icon>
            </AppBarButton>
        </CommandBar>

        <TextBlock Grid.Row="3"
                   Margin="0,14,0,0"
                   Text="Native pages now cover Auto Repair, Profile Cleanup, Event Logs, Windows Enhancements, Network Diagnostics, and Winget Maintenance."
                   TextWrapping="Wrap" />

        <TextBlock Grid.Row="4"
                   Margin="0,4,0,0"
                   Text="Next step is your wireframe pass. Once you drop that in, I can reshape the shell around the final layout instead of continuing functional conversion work."
                   TextWrapping="Wrap"
                   Style="{StaticResource SecondaryTextBlockStyle}" />
    </Grid>
</Page>
